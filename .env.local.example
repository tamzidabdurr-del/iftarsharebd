# ─── Firebase Configuration ───────────────────────────────────────────────────
# Firebase Console → Project Settings → Your apps → Web app → Config copy করুন

NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=iftarsharebd.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=iftarsharebd
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=iftarsharebd.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ─── Firebase Setup Steps ─────────────────────────────────────────────────────
#
# 1. Firebase Console: https://console.firebase.google.com
# 2. New Project → "iftarsharebd"
# 3. Add Web App → Config copy করুন → এখানে paste করুন
# 4. Firestore Database → Create (Production mode)
# 5. Rules:
#    rules_version = '2';
#    service cloud.firestore {
#      match /databases/{database}/documents {
#        match /iftar_locations/{doc} { allow read: if true; allow write: if true; }
#        match /volunteers/{doc} { allow read: if true; allow write: if true; }
#        match /help_requests/{doc} { allow read: if true; allow write: if true; }
#        match /eid_routes/{doc} { allow read: if true; allow write: if true; }
#        match /stats/{doc} { allow read: if true; allow write: if true; }
#      }
#    }
# 6. stats/daily document manually create করুন:
#    { total: 18450, volunteers: 0, locations: 12, help_fulfilled: 0 }

# ─── Vercel Deploy ────────────────────────────────────────────────────────────
# vercel env add NEXT_PUBLIC_FIREBASE_API_KEY
# (প্রতিটি variable এভাবে add করুন)
# vercel --prod
